import classNames from "classnames/bind"
import styles from "./TitleCards.moduls.scss"

const cx = classNames.bind(styles)
const TitleCards = () => {
  return (
    <div className={cx("title__cards")}>
      
    </div>
  )
}

export default TitleCards
